version: '3.7'

services:
  app:
    build:
      args:
        user: dinvoice-user
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: dinvoice-php
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./:/var/www
      - ./docker-compose/php/uploads.ini:/usr/local/etc/php/conf.d/uploads.ini:rw,delegated
    networks:
      - dinvoice

  db:
    image: mariadb
    restart: always
    volumes:
      - db:/var/lib/mysql
      # If you want to persist data on the host, comment the line above this one...
      # and uncomment the line under this one.
      #- ./docker-compose/db/data:/var/lib/mysql:rw,delegated
    environment:
      MYSQL_USER: dinvoice
      MYSQL_PASSWORD: dinvoice
      MYSQL_DATABASE: dinvoice
      MYSQL_ROOT_PASSWORD: dinvoice
    ports:
      - '33006:3306'
    networks:
      - dinvoice

  nginx:
    image: nginx:1.17-alpine
    restart: unless-stopped
    ports:
      - 80:80
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx:/etc/nginx/conf.d/
    networks:
      - dinvoice

volumes:
  db:

networks:
  dinvoice:
    driver: bridge
